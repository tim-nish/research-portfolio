---
slug: placeholder-validating-product
title: "[Placeholder] Validating product — replace before publishing"
status: validating
pain: >
  Placeholder pain statement seeding Epic 3's validating-lifecycle pipeline.
summary: >
  Placeholder product seeding Epic 3's product pipeline; replace with a real
  product before treating /products/ as content-complete.
platforms: [web]
cta: { kind: signup, href: "#newsletter", label: "Get notified" }
pricing: tbd
unlisted: true
---

**This is placeholder content**, not a real product. It exists to prove the
`validating` lifecycle state end to end (schema validation, and later the
`/products/<slug>/` template in Story 3.2). `unlisted: true` so it won't
appear on `/products/` once Story 3.3 builds that index.
