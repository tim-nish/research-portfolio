# Story 4.3: /ja/ JP landing page (gated)

**Status**: Not started — awaiting go/no-go
**Epic**: 4 — Compounding SEO
**Depends on**: Epic 1 (profile, identity links)

## Gate

Do not start without an explicit go decision (spec §10). This is scoped as a single
landing page, never a site mirror — that boundary is permanent (non-goal, spec §11),
not just a phase gate.

## Story

As a Japanese developer-community reader (P4), I want a lightweight JP landing
surface, so that I have a native-language bridge into the owner's work without a full
translated site existing.

## Acceptance criteria (once un-gated)

1. `/ja/` is a single page (not a route tree), presenting identity + a bridge to Zenn
   and to the (English) hub — consistent with AP-11 ("first-class content attribute,"
   not a parallel site).
2. No machine-translated mirrors are introduced anywhere on the site as part of this
   work (permanent non-goal, spec §11) — this story must not scope-creep into
   translating existing English pages.
3. URL space `/ja/` was already reserved since Phase 1 (spec §7.1) and unsquatted.

## Tasks / subtasks (once un-gated)

- [ ] Confirm go decision.
- [ ] Build the single `/ja/` page.

## Dev notes

- The permanent boundary in spec §11 ("Full Japanese site mirror... permanently out
  of scope unless this spec is revised") outranks this epic's own gating — even if
  `/ja/` is un-gated, it must never grow into a mirror without a spec revision.

## Gate decision log

- **2026-07-07: No-go.** No explicit go decision has been made yet (spec §10). No
  implementation was started. Re-evaluate when the owner decides to invest in a JP
  bridge surface — and if un-gated, scope must stay to the single `/ja/` page per
  AP-11/spec §11's permanent non-goal (no translated-site mirror).
- Confirmed no route conflict: `/ja/` is not referenced anywhere in
  `scripts/generate-static.tsx`'s generated routes or in `content/redirects.yml` —
  the URL space remains cleanly reserved (AC-3) for whenever this is un-gated.
- No code changed by this pass.
