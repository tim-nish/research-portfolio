# Story 1.11: Seed content migration & Phase 1 cutover

**Status**: Draft
**Epic**: 1 — Hub Foundation
**Depends on**: 1.1–1.10 (all Epic 1 stories)

## Story

As the site owner, I want the current site's content migrated into the new registry
and the old hardcoded page retired, so that the live site is the Phase 1 hub, not the
single-page app it replaces.

## Acceptance criteria

1. `content/profile.md` is authored from the current `App.tsx` profile text plus
   `profile.identity_links` seeded with GitHub (`tim-nish`), LinkedIn (`nowwest`),
   and email; Hugging Face/Zenn/X left as commented placeholders per spec D-7
   (Appendix A).
2. `content/projects/kagamios.md` authored: `kind: system` (per Appendix A's explicit
   resolution of the plugin-vs-system ambiguity — already decided in the spec, not
   re-litigated here), `status: active`, summary from the current card, GitHub link.
3. `content/projects/quantscenariobench.md` authored: `kind: benchmark`,
   `status: active`, `featured: true`, summary + three links (GitHub, HF leaderboard,
   HF datasets) from the current card; `citation` added if available, otherwise
   omitted (optional field).
4. `src/App.tsx`'s hardcoded profile/project markup is removed; the app shell now
   routes to the generated pages from Stories 1.2–1.10.
5. Full Phase 1 acceptance sweep: AC-1, AC-2, AC-3, AC-4, AC-5, AC-6, AC-7, AC-9 hold
   (spec §13); AC-8 holds at least for the `archived` project state (fixture if no
   real archived project exists).
6. `npm run build` (per `CLAUDE.md`) passes clean: type-check + content validation +
   static generation, with no console errors, before this story is called done.
7. Performance/accessibility budgets (NFR4, NFR5 — spec §8.5) are checked and recorded
   against at least the home page and one representative entity detail page: total
   transfer under ~200 KB (excluding embedded demo media, no external font/CDN
   dependency), semantic landmarks present, full keyboard operability, image alt text
   present, WCAG AA contrast. This is the one point in Phase 1 these NFRs are actually
   gated, not just aspirational — record results (tool used, pages checked, pass/fail)
   in the story's completion notes.

## Tasks / subtasks

- [ ] Author the three seed content records.
- [ ] Remove hardcoded content from `App.tsx`; confirm `src/components/ProjectCard.tsx`
      (or its successor) is now registry-driven.
- [ ] Run the full Phase 1 acceptance sweep (manual per-AC checklist) and record
      results.
- [ ] Confirm `npm run build`, `npm run preview`, and `docker compose up --build`
      (per `CLAUDE.md`) all still work against the new pipeline.
- [ ] Run a performance/accessibility spot-check (e.g. Lighthouse or equivalent) on
      the home page and one entity detail page; record page-weight and
      accessibility results against NFR4/NFR5.

## Dev notes

- This story is the Epic 1 "definition of done" gate — do not mark Epic 1 complete
  until this story's AC5/AC6 pass.

## Completion notes

- **AC1–3 (seed content):** already authored in Stories 1.2/1.3; re-verified verbatim
  against this story's exact wording — no changes needed.
- **AC4 (retire the SPA):** `src/App.tsx`, `src/main.tsx`, `src/components/ProjectCard.tsx`,
  and the old client-rendered `index.html` are deleted. `vite.config.ts`'s build entry
  is now `src/styles.css` directly — no JS bundle ships at all (every page is
  prerendered by `scripts/generate-static.tsx` via `react-dom/server`, none hydrate).
  Also removed the two dead CSS rules (`.eyebrow`, `.link-button`) that only the old
  SPA used.
- **AC5 (Phase 1 acceptance sweep, spec §13):**
  - AC-1: all Phase 1 §7.3 pages exist (Home, Projects index/detail, Benchmarks,
    Publications index/detail, About, Newsletter, 404, sitemap.xml, feed.xml); empty
    optional blocks confirmed rendering nothing (recent-writing, `now`, sections
    without hrefs). PASS
  - AC-2: repeatedly demonstrated across Stories 1.3/1.5/1.6/1.9 with temporary
    fixture records — one new content file, zero code changes. PASS
  - AC-3: URLs match §7.1's taxonomy; redirect (`/projects/prism/` →
    `/projects/kagamios/`) verified end-to-end via a local static server; `/404.html`
    renders identity + index links and is confirmed served for unmatched paths under
    `docker compose up --build`. PASS
  - AC-4: 9 dedicated fixture-driven failure-mode tests (`load.test.ts`) plus 1 more
    for `supersededBy`. PASS
  - AC-5: unique title/description/canonical/OG per page verified across all 8 live
    pages; JSON-LD present per applicable type; sitemap.xml (8 entries) and feed.xml
    both valid XML, feed additionally validated structurally against RFC 4287's
    required elements. PASS
  - AC-6: newsletter capture confirmed present on all 8 real content pages (footer)
    plus home's primary placement, `/newsletter/`, and the standard entity-page CTA
    block. (404/redirect stubs are intentionally chrome-free utility pages, not
    "content pages.") PASS
  - AC-7: every page ships zero required JS; verified by inspecting generated HTML
    (only two optional inline scripts exist — citation-copy and newsletter-embed
    fallback — neither gates content visibility). PASS
  - AC-8: `archived` project state — added permanent test coverage
    (`ProjectDetailPage.test.tsx`) asserting the lifecycle banner renders correctly
    and only for archived status; `groupProjectsForIndex` already proved archived
    projects separate from the active/featured bucket. PASS (scope: archived project
    only, per this story's explicit AC — sunset/superseded/deprecated are Phase
    2/3 concerns)
  - AC-9: J1 (artifact → identity) and J3 (name search → credibility) walked and
    verified live (newsletter capture reachable in 0–1 clicks from any entity page;
    home communicates identity in one screen with projects/publications/about
    reachable from nav). J2/J4 are not yet applicable — no `product` or `article`
    records exist until Epics 2/3. PASS (Phase 1 scope)
- **AC6 (clean build):** `npm run build` — validate-content, `tsc -b`, `vite build`,
  `generate-static.tsx` — all pass with no errors or console warnings.
- **AC7 (performance/accessibility spot-check, NFR4/NFR5):** No headless browser
  (Lighthouse) was available in this sandboxed environment, so an equivalent manual
  check was run instead — gzip byte-count measurement plus a programmatic WCAG 2.1
  contrast-ratio calculation, on the home page and `/projects/kagamios/`:
  - **Transfer size:** home ≈2.9 KB gzip (HTML + shared CSS), detail page ≈2.5 KB
    gzip — both far under the ~200 KB budget. No external font/CDN dependency (system
    font stack only, no `@font-face`/CDN `<link>`).
  - **Semantic landmarks:** `<header>`/`<nav>`/`<main>`/`<footer>` present on both
    pages.
  - **Keyboard operability:** every interactive element is a native `<a>` or
    `<button>` (no `onClick`/custom widgets in any `.tsx` file) — keyboard-operable
    by construction.
  - **Image alt text:** no `<img>` elements exist anywhere on the site yet — trivially
    satisfied, revisit once any story adds one.
  - **WCAG AA contrast:** every foreground/background color pair actually used in
    `styles.css` (7 pairs) checked against the WCAG 2.1 relative-luminance formula —
    lowest ratio found was 5.40:1, all comfortably clear of the 4.5:1 normal-text
    threshold.
  - Result: **PASS** on both pages checked.
- **Docker verification:** `docker compose up --build` was found genuinely broken
  before this story — the `Dockerfile` still copied `dist/` into a
  `/research-portfolio/` subpath left over from before the site moved to a
  root-based custom domain (`base: "/"`), so every root-absolute asset/page link
  404'd under that subpath. Fixed: `Dockerfile` now copies to nginx's document root,
  and a new `nginx.conf` adds `error_page 404 /404.html` so the Docker preview
  mirrors GitHub Pages' custom-404 behavior too. Verified via `docker exec curl`
  against the running container: home page, a project detail page, the redirect
  stub, sitemap/robots/feed, and a 404 path all resolve correctly.
